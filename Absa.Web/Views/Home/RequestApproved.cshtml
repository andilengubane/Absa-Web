@model Absa.Web.Models.ApplicationModel

@{
    ViewBag.Title = "RequestApproved";
}
@*<script type="text/javascript">
	function approveRequest() {
		var resilienceTrackID = $("#resilienceTrackID").val();
   			$.ajax({
   				type: "POST",
   				url: "/Home/UpdateRequestApproved",
   				data: '{resilienceTrackID: "' + resilienceTrackID + '" }',
   				contentType: "application/json; charset=utf-8",
   				dataType: "html",
   				success: function (response) {
   				},
   				failure: function (response) {
   					alert(response.responseText);
   				},
   				error: function (response) {
   					alert(response.responseText);
   				}
   		   });
   	}
</script>*@

@using (Html.BeginForm("UpdateRequestApproved", "Home", FormMethod.Post, new { id = "formDecline" }))
{
	<div>
		<dl class="dl-horizontal">
			@Html.HiddenFor(model => model.ResilienceID, new { id = "resilienceTrackID" })
			<dt>
				@Html.DisplayNameFor(model => model.BusinessUnit)
			</dt>

			<dd>
				@Html.DisplayFor(model => model.BusinessUnit)
			</dd>

			<dt>
				@Html.DisplayNameFor(model => model.ApplicationId)
			</dt>

			<dd>
				@Html.DisplayFor(model => model.ApplicationId)
			</dd>

			<dt>
				@Html.DisplayNameFor(model => model.FullName)
			</dt>
		</dl>
		<br>
		<div class="form-row">
			<div class="form-group col-md-6">
				<input type="submit" value="Submit" class="btn btn-circle btn-success btn-lg btn-block"  return false" /> 
			</div>
		</div>
	</div>
}